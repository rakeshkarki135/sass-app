services:
  db:
    image: postgres
    volumes:
      - ./data/db:/var/lib/postgres/data
    env_file:
      - ./CoursePlatform/.env

  web:
    build: ./CoursePlatform
    ports: 
      - "8080:8000"
    env_file:
      - ./CoursePlatform/.env
    
    depends_on:
      - db